<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:id="@+id/mainscreen">

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/goal_spinner"
        android:layout_below="@+id/goal_edit"
        android:layout_alignEnd="@+id/goal_edit"
        android:spinnerMode="dropdown"
        android:entries="@array/goalchoice"
        />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/goal_edit"
        android:id="@+id/goal_edit"
        android:layout_alignBottom="@+id/goal_button"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/goal_button"
        android:inputType="text"
        android:imeActionLabel="@string/goal_enter"

        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/goal_edit"
        android:id="@+id/goal_button"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        />


</RelativeLayout>
